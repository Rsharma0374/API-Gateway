apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
spec:
  type: NodePort
  selector:
    app: api-gateway-service
  ports:
    - protocol: TCP
      port: 10008
      targetPort: 10008
      nodePort: 30008